# 查询重复医嘱模块实现说明

本文档描述 `统计分析管理 -> 免疫重复项目统计(&3)` 的当前实现。模块面向业务人员回答一个直接问题：在选定的签收时间范围内，哪些住院患者同时存在基准医嘱和指定的重复医嘱。

## 功能边界

- 仅查询住院申请，不处理门诊申请。
- 仅查询已签收且有效的申请记录。
- 使用 `YJ_ZYSQ.YZXMID` 精确识别项目，不使用 `SQNR` 文字匹配，也不存在代码缺失时的文字兜底。
- 按 `INPATIENT_ID` 限定同一次住院，可发现同条码和跨条码的重复医嘱。
- 全程只读，不修改、取消或回写 LIS 业务数据。
- 当前不提供导出、床号回填和报告跳转。

## 用户操作流程

1. 选择签收开始、结束时间。默认是含当天在内的最近 7 个自然日，即 6 天前 `00:00` 至当天 `23:59`。
2. 点击“查询重复医嘱”。
3. 在患者列表查看命中患者，并通过姓名、病人号或条码搜索。
4. 使用“全部 / 同条码 / 跨条码”筛选重复关系。
5. 选择一位患者，在下方核对其基准医嘱和重复医嘱明细。
6. 如需调整项目范围，打开“规则设置”，从当前时间段的项目记录中选择或直接填写 `YZXMID`。

## 页面结构

```text
┌──────────────────────────────────────────────────────────┐
│ 签收时间 [开始] 至 [结束]  [查询重复医嘱] [规则设置]       │
│ 关系 [全部/同条码/跨条码]  搜索 [姓名、病人号或条码]       │
├──────────────────────────────────────────────────────────┤
│ 涉及患者       重复医嘱       同条码       跨条码           │
├──────────────────────────────────────────────────────────┤
│ 患者列表：每次住院一行                                    │
├──────────────────────────────────────────────────────────┤
│ 选中患者概览                                               │
│ 基准医嘱（绿色）                                           │
│ 重复医嘱：同条码（蓝色）/ 跨条码（橙色）                   │
└──────────────────────────────────────────────────────────┘
```

患者列表用于定位问题，医嘱列表用于核查原因。这样避免原先“一条申请占一行”造成同一患者重复出现，也减少用户在多列数据中自行拼接关系的负担。

## 项目规则

### 默认值

| 规则 | 默认 `YZXMID` | 用途 |
| --- | --- | --- |
| 基准项目 | `12731` | 标记已经覆盖相关检测需求的医嘱 |
| 重复项目 | `8714;8724;7345` | 标记需要人工核对的其他医嘱 |

### 输入校验

- 只接受数字代码以及逗号、分号、空格等分隔符。
- 保存和查询前都会去重。
- 基准项目与重复项目不能为空，也不能包含相同代码。
- `SQNR` 只在规则选择和结果页辅助用户辨认项目，不参与查询判断。

### 本地持久化

规则保存在程序目录的 `ClientConfig.ini`：

```ini
[ImmuneDuplicateStatistics]
BaseYzxmidCodes=12731
DuplicateYzxmidCodes=8714;8724;7345
```

该配置只影响当前客户端，不写入数据库。用户保存规则后，下一次进入模块会自动加载。

### 项目记录选择器

项目中尚未确认独立且稳定的医嘱项目主数据表，因此规则设置窗口从当前签收时间范围的 `YJ_ZYSQ` 记录生成候选项：

```sql
SELECT TOP 1000
       YZXMID,
       SQNR,
       COUNT_BIG(*) AS RECORD_COUNT,
       MAX(JSSJ) AS LAST_SIGN_TIME
FROM YJ_ZYSQ WITH (NOLOCK)
WHERE JSSJ >= ?
  AND JSSJ < DATEADD(minute, 1, ?)
  AND JSSJ IS NOT NULL
  AND ZXKS IN (102, 401)
  AND (BSCBZ IS NULL OR BSCBZ = 0)
  AND YZXMID IS NOT NULL
GROUP BY YZXMID, SQNR
ORDER BY COUNT_BIG(*) DESC, YZXMID
```

用户可查看项目代码、医嘱名称、记录数和最近签收时间，再把选中代码加入基准或重复集合。

## 数据关系

| 数据表/字段 | 当前用途 |
| --- | --- |
| `YJ_ZYSQ.YJSQID` | 申请明细稳定去重键 |
| `YJ_ZYSQ.INPATIENT_ID` | 同一次住院的查询和聚合键 |
| `YJ_ZYSQ.YZXMID` | 基准、重复项目的唯一匹配字段 |
| `YJ_ZYSQ.TXM` | 判断同条码或跨条码 |
| `YJ_ZYSQ.JSSJ` | 签收时间筛选和展示 |
| `YJ_ZYSQ.SQRQ` | 申请时间展示 |
| `YJ_ZYSQ.BBCJSJ` | 采集时间展示 |
| `YJ_ZYSQ.SQNR` | 医嘱名称展示，不参与匹配 |
| `ZY_INPATIENT.INPATIENT_NO` | 病人号展示 |
| `ZY_INPATIENT.NAME` | 患者姓名展示 |
| `JC_DEPT_PROPERTY.DEPT_NAME` | 申请科室名称展示 |

`YJ_ZYSQ.INPATIENT_ID = ZY_INPATIENT.INPATIENT_ID` 用于关联同一次住院。系统不使用姓名、身份证、床号或模糊住院号替代这个关系，避免把不同患者或不同住院次数错误合并。

## 查询逻辑

### 1. 查询基准申请

```sql
SELECT ...
FROM YJ_ZYSQ y WITH (NOLOCK)
LEFT JOIN ZY_INPATIENT z WITH (NOLOCK)
       ON z.INPATIENT_ID = y.INPATIENT_ID
LEFT JOIN JC_DEPT_PROPERTY d WITH (NOLOCK)
       ON d.DEPT_ID = y.SQKS
WHERE y.JSSJ >= ?
  AND y.JSSJ < DATEADD(minute, 1, ?)
  AND y.JSSJ IS NOT NULL
  AND y.ZXKS IN (102, 401)
  AND (y.BSCBZ IS NULL OR y.BSCBZ = 0)
  AND y.YZXMID IN (用户配置的基准代码)
```

### 2. 查询重复申请

```sql
SELECT ...
FROM YJ_ZYSQ y WITH (NOLOCK)
LEFT JOIN ZY_INPATIENT z WITH (NOLOCK)
       ON z.INPATIENT_ID = y.INPATIENT_ID
LEFT JOIN JC_DEPT_PROPERTY d WITH (NOLOCK)
       ON d.DEPT_ID = y.SQKS
WHERE y.JSSJ >= ?
  AND y.JSSJ < DATEADD(minute, 1, ?)
  AND y.JSSJ IS NOT NULL
  AND y.ZXKS IN (102, 401)
  AND (y.BSCBZ IS NULL OR y.BSCBZ = 0)
  AND y.YZXMID IN (用户配置的重复代码)
```

重复申请查询不使用关联子查询或逐行 `EXISTS`。程序先取得基准申请的 `INPATIENT_ID` 集合，再在内存中保留属于这些住院的重复申请。这样既规避 SQL Server 聚合表达式外部引用错误，也减少慢关联子查询带来的执行压力。

### 3. 归组与去重

- 先按 `YJSQID` 去重申请明细。
- 再按 `INPATIENT_ID` 归为一次住院；仅展示同时具有基准和重复申请的患者。
- 重复申请的 `TXM` 与任一基准条码相同，标记为“同条码”。
- 重复申请的 `TXM` 与全部基准条码不同，标记为“跨条码”。
- 一个患者可以同时存在两种关系，患者列表显示组合状态。

## 统计口径

| 指标 | 计算方式 |
| --- | --- |
| 涉及患者 | 同时命中基准和重复项目的 `INPATIENT_ID` 数量 |
| 重复医嘱 | 去重后的重复申请条数 |
| 同条码 | 与该患者任一基准条码相同的重复申请条数 |
| 跨条码 | 与该患者全部基准条码不同的重复申请条数 |

患者搜索与关系筛选只改变当前页面展示范围，不会重新访问数据库，也不会改变原始查询结果。

## 性能与安全

- 所有业务查询均为只读，并沿用项目现有的 `WITH (NOLOCK)` 查询方式。
- 时间和项目代码在 SQL 层过滤，避免读取完整申请表。
- 项目代码经严格数字校验后才拼入 `IN (...)`，时间文本在拼接 SQL 前执行转义。
- 查询在线程中执行，结果统一回到 UI 线程渲染，避免阻塞主窗口。
- 列表批量更新时暂停重绘，减少大结果集下的界面闪烁。
- 默认 7 天范围用于兼顾日常核查覆盖面和查询性能；扩大时间范围时，耗时取决于现场数据量和数据库索引。

## 当前限制与后续核实

- 尚未确认稳定的医嘱项目主数据字典，项目选择器暂以实际申请记录为依据。
- 床号、检测结果、报告时间和报告跳转字段尚未接入。
- 当前不区分收费、退费或取消状态；在业务字段含义明确前不擅自增加过滤条件。
- 当前不导出明细。如后续恢复导出，应以患者核查结构为基础单独设计，不复用旧页面的隐藏入口。
- MinGW Windows 交叉编译已作为静态验证；项目代码和统计口径仍需在真实 Windows 数据库环境进行命中核对。
