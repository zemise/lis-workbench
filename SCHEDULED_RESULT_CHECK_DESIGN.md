# 定时核查结果模块设计

## 1. 文档目的

本文档记录 `定时任务 -> 定时核查结果` 模块的首版需求、数据口径、规则模型、扫描流程、提醒机制和后续开发边界，作为实现、测试和验收依据。

首版模块已按本文档实现；后续需求变更应同步更新本文档、核心测试和验收口径。

## 2. 建设目标

在主程序运行期间，每分钟自动只读核查当日 LIS 检验结果。新规则必须绑定检验科室和仪器，每轮按该仪器扫描当日目标项目；同仪器多规则合并为一次结果查询。升级前未绑定仪器的旧规则继续走全局 `REP_NO` 增量、未完成报告复查和分段巡检路径，不会自动缩窄范围。用户可维护项目间的结果比较规则；当同一个 `REP_NO` 下的两个目标项目同时存在有效结果，并满足配置关系时，程序向用户弹窗提醒。

示例规则：

```text
项目 A：离子钙（ITEM_CODE=92882）
关系：小于
项目 B：钾（ITEM_CODE=92879）

触发条件：同一 REP_NO 中，离子钙 RESULT < 钾 RESULT
```

模块用于辅助发现项目间可能存在的逻辑矛盾或需要人工复核的结果，不替代 LIS 审核规则、危急值判断或人工审核。

## 3. 已确认需求

1. 顶级入口位于菜单栏 `定时任务` 下，模块名称为 `定时核查结果`。
2. 扫描周期首版固定为 1 分钟，不开放周期配置。
3. 核查范围始终是当日结果，但常规轮次不重复读取当天全量；不使用“最近一小时”作为固定业务范围。
4. 用户可以维护多条核查规则，并可启用或停用单条规则。
5. 规则两侧项目来自 LIS 项目字典；用户既可从列表选择，也可直接输入完整 `ITEM_CODE` 精确匹配。
6. 规则按同一个 `REP_NO` 关联项目明细。
7. LIS 数据库仅允许执行 `SELECT`，规则、执行状态和提醒历史保存在本机。
8. 新命中通过 Windows 通知区域汇总提醒；所有未处理记录持续显示在系统置顶悬浮窗中，并在模块页面保留待处理记录和今日已处理记录。

## 4. 数据来源与字段口径

### 4.1 报告主表

使用 `LS_AS_REPORT` 确定报告、当日范围和跳转常规报告所需信息。

主要字段：

| 字段 | 用途 |
| --- | --- |
| `REP_NO` | 报告唯一关联键 |
| `CHK_DATE` | 当日扫描范围字段 |
| `OPER_NO` | 样本号，用于显示和跳转 |
| `MACH_CODE` | 仪器代码 |
| `ROOM_CODE` | 专业组/检验科室代码 |
| `DELETE_BIT` | 删除标志 |

当日范围采用左闭右开条件：

```sql
r.CHK_DATE >= CONVERT(date, GETDATE())
AND r.CHK_DATE < DATEADD(day, 1, CONVERT(date, GETDATE()))
```

`CHK_DATE` 在现场样本中只有日期精度，但适合限定“当日”范围；不能依靠它识别一分钟内新增或更新的明细。

### 4.2 结果明细表

使用 `LS_AS_REPENTRY` 读取同一报告下的项目和结果。

主要字段：

| 字段 | 用途 |
| --- | --- |
| `ID` | 明细行标识，用于追踪和排查 |
| `REP_NO` | 关联 `LS_AS_REPORT.REP_NO` |
| `ITEM_CODE` | 规则匹配的项目唯一标识 |
| `ITEM_NAME` | 项目名称兜底显示 |
| `RESULT` | 待比较的原始结果文本 |
| `DELETE_BIT` | 删除标志 |

### 4.3 项目字典

项目选择器先按仪器读取 `LS_AS_GROUP + LS_AS_GROUP_ITEM` 的项目代码，再与 `LS_AS_ITEM` 取交集：

| 字段 | 用途 |
| --- | --- |
| `ITEM_CODE` | 规则实际保存和匹配的项目代码 |
| `ITEM_NAME` | 中文项目名称 |
| `ENG_NAME` | 英文名称，参与搜索和显示 |
| `UNIT` | 单位，帮助用户确认项目 |
| `DELETE_BIT` | 只加载未删除项目 |

项目显示名称优先使用 `LS_AS_ITEM.ITEM_NAME`，字典缺失时回退 `LS_AS_REPENTRY.ITEM_NAME`。

规则必须以 `ITEM_CODE` 匹配，不能只按 `ITEM_NAME` 匹配。实际样本中相同名称可能对应不同仪器或不同项目代码，例如“PH值”存在 `92225` 和 `92457` 两个代码。

## 5. 现场样本分析结论

对用户提供的 1000 条样本数据分析结果如下：

| 指标 | 结果 |
| --- | ---: |
| 明细行数 | 1000 |
| 不同 `REP_NO` 数 | 78 |
| `RESULT` 为空 | 620 |
| `RESULT` 非空 | 380 |
| 纯数值结果 | 126 |
| 定性或特殊格式结果 | 254 |
| `CREATE_TIME` 为空 | 109 |
| `REP_TIME` 为空 | 985 |
| 同一 `REP_NO + ITEM_CODE` 重复 | 0 |

已观察到的非数值结果包括：

```text
阴性、阳性、未见、-、+-、1+、2+、3+、< 10.00、软、黄褐色
```

由此确定：

1. 报告建立时可能先生成空结果行，之后再更新同一行的 `RESULT`。
2. `LS_AS_REPENTRY.ID` 不会随结果更新而变化，不能只按新增 ID 做增量扫描。
3. `CREATE_TIME` 和 `REP_TIME` 均不能覆盖全部待核查场景。
4. 首版采用每分钟重复查询当日目标项目；后续现场固定编号对照显示，部分报告在创建约 9 分钟后补入明细或在原明细上填入结果，因此不能仅使用最大 `REP_NO` 或最大明细 `ID` 作为唯一增量依据。
5. 空结果和不可解析结果必须跳过，不能当作 `0`。

## 6. 规则模型

每条规则包含以下字段：

| 字段 | 说明 |
| --- | --- |
| `id` | 本地规则 ID |
| `name` | 规则名称，允许用户自定义 |
| `enabled` | 是否启用 |
| `left_item_code` | 项目 A 代码 |
| `left_item_name` | 项目 A 名称快照 |
| `left_item_unit` | 项目 A 单位快照 |
| `operator` | 比较运算符 |
| `right_item_code` | 项目 B 代码（固定值模式为空） |
| `right_item_name` | 项目 B 名称快照（固定值模式为空） |
| `right_item_unit` | 项目 B 单位快照（固定值模式为空） |
| `compare_with_value` | 是否为固定值模式；为 `true` 时右侧使用 `right_value_text` |
| `right_value_text` | 固定值阈值文本，如 `90.0`、`-1.5` |
| `created_at` | 创建时间 |
| `updated_at` | 最后修改时间 |

首版支持以下数值关系：

| UI 名称 | 存储值 | 判断 |
| --- | --- | --- |
| 大于 | `>` | `A > B` 或 `A > 固定值` |
| 大于等于 | `>=` | `A >= B` 或 `A >= 固定值` |
| 小于 | `<` | `A < B` 或 `A < 固定值` |
| 小于等于 | `<=` | `A <= B` 或 `A <= 固定值` |
| 等于 | `=` | `A == B` 或 `A == 固定值` |
| 不等于 | `!=` | `A != B` 或 `A != 固定值` |

`compare_with_value=true` 时右侧为固定值常量，固定值在保存时校验为有效数字。加减乘除公式、多项目表达式、参考范围比较、定性结果顺序比较或跨报告比较仍不在首版范围内，可在稳定后另行设计。

## 7. 结果解析规则

### 7.1 可比较结果

首版只接受去除首尾空格后能够完整解析为有限十进制数的 `RESULT`，包括：

```text
0
1.25
-8.81
+3.5
```

解析必须消费完整字符串，不能只读取字符串开头的数字。

### 7.2 不可比较结果

以下情况不参与首版比较：

- 空字符串或仅空白；
- `NULL`；
- `< 10.00`、`>100` 等带界限符号的结果；
- `阴性`、`阳性`、`未见`；
- `-`、`+-`、`1+`、`2+`、`3+`；
- 数字和文字混合结果；
- 非有限数值。

不可比较不是规则命中，不弹窗；扫描核心会累计本轮跳过数量，供后续诊断展示使用。

### 7.3 浮点相等

首版 `=` 和 `!=` 不应直接使用二进制浮点绝对相等。实现时采用统一的近似相等策略，例如：

```text
abs(A - B) <= max(1e-9, max(abs(A), abs(B)) * 1e-9)
```

最终容差应在核心逻辑单元测试中固定。

## 8. 每分钟扫描流程

监控属于主程序级后台能力，不能依赖模块页面保持打开。只要主程序正在运行且存在启用规则，就应执行扫描。

流程如下：

1. 主程序启动后加载本地启用规则。
2. 建立 60 秒定时器；首次执行时也走同一防重入入口。
3. 若上一轮仍在运行，本轮直接跳过，禁止并发扫描。
4. 收集所有启用规则涉及的 `ITEM_CODE` 并去重。
5. 新规则每轮按 `LS_AS_REPORT.ROOM_CODE + MACH_CODE` 查询该仪器当日全部有效目标结果，不依赖编号回看；同仪器规则去重项目代码后只查询一次。旧规则在启动、规则变更、手动“立即扫描”和跨日时查询当天全部有效报告；常规轮次查询新增编号及最近 100 个编号、到期的未完成报告，并轮巡当天约十五分之一的编号范围。
6. 在内存中按 `REP_NO` 分组，再按 `ITEM_CODE` 建立项目结果索引。
7. 对每个 `REP_NO` 逐条执行适用规则：
   - 两侧项目均存在；
   - 两侧 `RESULT` 均非空；
   - 两侧均可解析为数值；
   - 运算关系成立。
8. 根据本地命中历史判断是否为新命中。
9. 保存新命中记录。
10. 成功保存提醒后持久化扫描游标和未完成报告列表；进度保存失败时下轮重查，不吞掉本轮已生成的提醒。
11. UI 线程弹出一次汇总提醒，并刷新已打开的模块页面。

扫描 SQL 下推日期、删除状态、项目代码，以及新规则的 `ROOM_CODE + MACH_CODE` 或旧规则的 `REP_NO` 范围/待复查编号集合；项目配对、数值解析和规则判断在 C++ 内存中完成。仅旧规则的新报告查询使用左连接识别尚无目标明细的报告；旧规则未完成报告及分段轮巡逻辑不变。多台工作站仍各自扫描，此优化不提供跨机共享或单实例协调。

全量查询方向（增量轮次另加 `REP_NO` 范围或编号条件；只轮巡有目标项目的报告时使用内连接）：

```sql
SELECT
    e.ID,
    r.REP_NO,
    r.OPER_NO,
    r.ROOM_CODE,
    r.MACH_CODE,
    r.CHK_DATE,
    m.MACH_NAME,
    e.ITEM_CODE,
    e.ITEM_NAME,
    e.ITEM_ENG,
    e.RESULT
FROM LS_AS_REPORT r WITH (NOLOCK)
LEFT JOIN LS_AS_REPENTRY e WITH (NOLOCK)
    ON e.REP_NO = r.REP_NO
   AND e.DELETE_BIT = 0
   AND e.ITEM_CODE IN (...)
LEFT JOIN LS_AS_MACHINE m WITH (NOLOCK)
    ON m.MACH_CODE = r.MACH_CODE
   AND ISNULL(m.DELETE_BIT, 0) = 0
WHERE r.DELETE_BIT = 0
  AND r.CHK_DATE >= CONVERT(date, GETDATE())
  AND r.CHK_DATE < DATEADD(day, 1, CONVERT(date, GETDATE()))
ORDER BY r.REP_NO, e.ITEM_CODE, e.ID;
```

项目字典 `LS_AS_ITEM` 按数据库连接在进程内缓存 30 分钟；扫描时对项目代码统一去除首尾空格，再用缓存补全项目中文名和 `ENG_NAME`，字典不可用时回退明细中的 `ITEM_NAME / ITEM_ENG`，不影响数值核查。结果查询不再逐次关联项目字典。字典加载失败时在页面状态开头提示，不静默忽略。

所有文本条件必须使用项目现有 SQL 转义方式，数据库连接、超时、日志脱敏和后台任务生命周期沿用现有公共实现。

## 9. 重复项目处理

当前样本中未发现同一 `REP_NO + ITEM_CODE` 多行，但实现不能依赖这一现象。

若实际查询出现重复，首版建议按以下顺序选定一条：

1. 结果非空优先；
2. `ID` 较大的行优先；
3. 若仍无法唯一确定，记录诊断信息并使用最终选定行参与判断。

模块页面的命中明细应保存实际使用的左右两侧 `ENTRY_ID`，便于追溯。

## 10. 提醒与去重

### 10.1 去重键

命中指纹至少包含：

```text
rule_id
REP_NO
left_entry_id
right_entry_id
规范化后的左结果
规范化后的右结果
```

同一指纹只提醒一次。若任一项目结果发生变化，则生成新指纹并重新判断；变化后仍命中时可以再次提醒。

规则内容修改并保存时，必须在同一 SQLite 事务中清除该规则此前产生的全部命中记录和观察指纹，然后按修改后的规则重新扫描，避免新旧规则结果混杂。删除规则时也必须同时清除其全部命中记录和观察指纹。停用规则时同样清除该规则的命中记录和观察指纹，并立即刷新悬浮提醒；再次启用后重新扫描当日结果。保存扫描结果前还要复核规则内容及启用状态仍与本轮扫描快照一致，防止更新、停用或删除前已经启动的后台扫描重新写入旧规则结果。

### 10.2 首次扫描

新建规则后的首次扫描必须核查当天全部报告，不区分审核或发送状态：

- 不使用 `CHK_FLAG` 或 `CONF` 过滤报告；
- 已审核、未审核、已发送和未发送报告均参与；
- 两侧项目均存在有效数值且满足规则时，首次扫描直接生成提醒；
- 已观察指纹只用于抑制后续每分钟扫描中的相同结果，不得把首次扫描静默处理；
- 主程序重启后仍保留已观察指纹，避免同一规则、报告和结果重复提醒；结果变化后生成新指纹并重新判断。

### 10.3 分层提醒

一轮扫描出现多条新命中时只发送一条 Windows 通知区域汇总提醒，并播放一次警告音、闪烁任务栏按钮，避免连续多个模态 `MessageBox` 阻塞操作。通知由 Win32 `Shell_NotifyIcon` 提供，以兼容当前未打包桌面程序；点击通知或通知区域图标打开 `定时核查结果` 页面。

汇总通知包括：

- 新命中数量；
- 规则名称；
- 项目 A 名称和结果；
- 运算符；
- 项目 B 名称和结果。

通知正文展示新增数量和一条代表性结果；详细报告号、样本号和发现时间保留在页面列表中。

只要本机 SQLite 中仍存在未标记处理的记录，桌面工作区右下角就显示一个 `WS_EX_TOPMOST + WS_EX_TOOLWINDOW + WS_EX_NOACTIVATE` 悬浮待办窗：

- 不抢占当前输入焦点；
- 跨日期累计全部未处理记录；
- 展开时显示最近 3 条，每条以 `LS_AS_REPORT.OPER_NO` 样本号开头（为空时显示“样本号未记录”），核查关系优先显示项目英文名、缺失时回退中文名、两者均缺失时才显示项目代码，右侧提供“标记已处理”；悬浮窗宽度为 390 逻辑像素，长文本省略显示；收起时保留待处理数量；
- 点击每条的“标记已处理”仅更新本机待处理状态并刷新悬浮窗及已打开的列表，不唤起工作台；
- 点击悬浮窗记录的其他区域时，将 LIS 工作台最大化并打开对应常规报告，同时在结果 ListView 中把该记录涉及的项目代码所在行以橙色背景标记；
- 点击悬浮窗内容区其他位置（含“点击打开待处理列表”页脚）时，将 LIS 工作台最大化并打开待处理列表；
- 拖动悬浮窗标题区域可调整摆放位置，位置保存到本机配置并在重启后恢复，拖动范围限制在当前显示器工作区内；
- 展开/收起切换保持窗口底边固定，避免因内容高度变化导致位置漂移；
- 根据主程序所在显示器的工作区定位，不遮挡任务栏；
- 全部处理后自动隐藏，并移除通知区域图标；
- 程序重启后从 SQLite 恢复。

## 11. 本地存储

建议使用独立 SQLite 文件 `scheduled_result_check.sqlite`，不与 LIS 业务库混用，也不依赖 `ClientConfig.ini` 保存结构化规则。

### 11.1 规则表

```sql
CREATE TABLE IF NOT EXISTS scheduled_result_rule (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    enabled INTEGER NOT NULL DEFAULT 1,
    left_item_code TEXT NOT NULL,
    left_item_name TEXT NOT NULL DEFAULT '',
    left_item_unit TEXT NOT NULL DEFAULT '',
    operator TEXT NOT NULL,
    right_item_code TEXT NOT NULL,
    right_item_name TEXT NOT NULL DEFAULT '',
    right_item_unit TEXT NOT NULL DEFAULT '',
    compare_with_value INTEGER NOT NULL DEFAULT 0,
    right_value_text TEXT NOT NULL DEFAULT '',
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);
```

### 11.2 命中记录表

```sql
CREATE TABLE IF NOT EXISTS scheduled_result_alert (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    rule_id INTEGER NOT NULL,
    rule_name TEXT NOT NULL DEFAULT '',
    rep_no TEXT NOT NULL,
    oper_no TEXT NOT NULL DEFAULT '',
    room_code TEXT NOT NULL DEFAULT '',
    mach_code TEXT NOT NULL DEFAULT '',
    mach_name TEXT NOT NULL DEFAULT '',
    inspect_date TEXT NOT NULL DEFAULT '',
    left_entry_id TEXT NOT NULL DEFAULT '',
    left_item_code TEXT NOT NULL,
    left_item_name TEXT NOT NULL DEFAULT '',
    left_item_eng TEXT NOT NULL DEFAULT '',
    left_result_text TEXT NOT NULL,
    left_result_value REAL,
    operator TEXT NOT NULL,
    right_entry_id TEXT NOT NULL DEFAULT '',
    right_item_code TEXT NOT NULL,
    right_item_name TEXT NOT NULL DEFAULT '',
    right_item_eng TEXT NOT NULL DEFAULT '',
    right_result_text TEXT NOT NULL,
    right_result_value REAL,
    compare_with_value INTEGER NOT NULL DEFAULT 0,
    fingerprint TEXT NOT NULL,
    handled INTEGER NOT NULL DEFAULT 0,
    handled_at TEXT,
    discovered_at TEXT NOT NULL,
    UNIQUE(rule_id, fingerprint),
    FOREIGN KEY(rule_id) REFERENCES scheduled_result_rule(id) ON DELETE CASCADE
);
```

### 11.3 观察指纹表

```sql
CREATE TABLE IF NOT EXISTS scheduled_result_observation (
    rule_id INTEGER NOT NULL,
    fingerprint TEXT NOT NULL,
    observed_at TEXT NOT NULL,
    PRIMARY KEY(rule_id, fingerprint),
    FOREIGN KEY(rule_id) REFERENCES scheduled_result_rule(id) ON DELETE CASCADE
);
```

### 11.4 扫描进度与未完成报告

本机 SQLite 另存 `scheduled_result_scan_progress`（数据库日期、规则快照、最大已扫描 `REP_NO`、当日最小编号和轮巡进度）及 `scheduled_result_pending_report`（编号、首次观察时间、下次复查时间）。规则快照或日期变化时重新全量扫描；进度仅在源查询及命中记录写入成功后推进。对最近编号中目标项目缺失、空值或非数值的报告保留待复查状态；超过快速窗口的报告仍由分段轮巡兜底。该状态只优化本机读取，不共享规则和提醒。

可增加索引：

```sql
CREATE INDEX IF NOT EXISTS idx_scheduled_alert_day
ON scheduled_result_alert(discovered_at, handled);

CREATE INDEX IF NOT EXISTS idx_scheduled_alert_rep
ON scheduled_result_alert(rep_no, rule_id);

CREATE INDEX IF NOT EXISTS idx_scheduled_alert_pending
ON scheduled_result_alert(handled, id DESC);
```

本地库保存项目名称、英文名和单位快照，以免字典后续改名导致历史提醒无法还原。旧命中记录通过新增列的空默认值兼容；旧记录不会因重复扫描而重建，若英文名为空，悬浮窗回退显示中文名。更新规则会清除旧命中，重新扫描得到的新提醒才会保存英文名。早期数据库中可能仍存在 `baseline_ready` 兼容列；当前代码不再读取或写入该列，新建数据库不再创建它。

## 12. 项目选择器

新建或编辑规则时，项目 A 始终使用项目选择器；右侧支持两种模式：

- 默认“项目 B”模式使用项目选择器；
- 勾选“与固定值比较”后，右侧切换为固定值输入框，仅项目 A 参与选择，固定值在保存时校验为有效数字。

选择器要求：

1. 首次打开时后台读取 `LS_AS_ITEM` 字典；选择仪器后后台读取对应 `LS_AS_GROUP_ITEM` 项目代码，不阻塞 UI。
2. 只显示所选仪器配置、且 `ISNULL(LS_AS_ITEM.DELETE_BIT, 0)=0` 的项目；未选仪器时不显示项目。
3. 显示列为项目代码、中文名、英文名和单位。
4. 支持按项目代码、中文名和英文名本地搜索。
5. 在项目 A 或项目 B 输入框直接键入完整 `ITEM_CODE` 时，仅在该仪器可选项目内精确匹配，并回填项目名称、英文名和单位；存在更长的同前缀代码时，等待输入框失焦后再确认短代码，避免提前误选。未命中的纯数字代码不得按子串匹配到其他项目。
6. 默认按项目代码、项目名称排序。
7. 对同名不同代码项目分别展示，禁止按名称自动合并；同一 `ITEM_CODE` 的重复字典行在加载时合并为一个选项。
8. 数据库未配置、项目字典或仪器映射加载失败、交集为空时给出明确错误，并禁止保存；不回退到全字典。

项目字典在进程内按数据库连接隔离缓存，30 分钟后重新加载；数据库连接配置变化时使用新连接对应的缓存，不混用旧连接项目。

## 13. 页面设计

页面默认以待处理工作台为主，规则设置按需展开。未处理记录跨日期保留，已处理记录只显示当日数据。

### 13.1 核查规则区

默认收起时只显示：

- 规则设置；
- 立即扫描。

展开规则设置后显示：

- 新建规则；
- 选中规则后编辑并更新；
- 删除规则；
- 启用/停用；
- 立即扫描。

规则列表列：

- 状态；
- 规则名称；
- 检验仪器；
- 项目 A；
- 关系；
- 项目 B。

固定说明文字：`仪器规则每分钟核查该仪器当日结果；旧规则沿用增量轮巡。LIS 数据库只读。`

### 13.2 待处理区

列表列：

- 发现时间；
- 处理状态；
- 规则名称；
- 报告号；
- 样本号；
- 项目 A；
- A 结果；
- 关系；
- 项目 B；
- B 结果。

支持：

- 标记已处理；
- 双击跳转常规报告。

未处理行使用浅橙色背景和深色文字，列表标题直接显示待处理数量；处理后记录从悬浮窗移除并保留在当天页面中。

首版不提供修改 LIS 结果、自动撤销审核、自动发送消息或远程通知。

## 14. 常规报告跳转

双击命中记录时复用现有 `RegularReportOpenTarget + WM_REGULAR_OPEN_REPORT` 机制。

扫描结果应保留：

- `REP_NO`；
- `OPER_NO`；
- `CHK_DATE`；
- `MACH_CODE`；
- 仪器名称（可通过现有字典查询补充）；
- `ROOM_CODE`。

跳转只定位报告，不修改任何 LIS 数据。

## 15. 后台任务与生命周期

1. 使用现有 `WindowTask` 模式执行数据库查询，禁止 UI 线程直接查询。
2. 同一时间只允许一个自动或手动扫描任务运行。
3. 手动点击“立即扫描”或规则更新触发重扫时，如果已有扫描正在执行，则登记一次待重扫请求；当前任务结束后立即按最新规则再扫一次，不并发启动第二个任务。
4. 主窗口销毁时取消任务结果所有权，禁止旧结果回写已销毁窗口。
5. 模块页面关闭不停止主程序级扫描。
6. 数据库连接未配置、连接失败或查询失败时不弹业务命中窗口；模块页面显示失败状态，并在下一周期重试。
7. 单次失败不停止后续每分钟调度。

## 16. 日志与隐私

日志允许记录：

- 扫描开始和结束时间；
- 启用规则数；
- 查询返回行数和报告数；
- 可比较、不可比较、命中和新增提醒数量；
- 查询耗时；
- 脱敏错误摘要。

日志不得记录：

- 完整 SQL；
- 数据库连接串和密码；
- 患者姓名、病人号等身份信息；
- 全量原始结果。

命中页面只展示完成核查和定位报告所需的最少字段。

## 17. 性能原则

1. 每次扫描只查询启用规则涉及的去重 `ITEM_CODE` 集合。
2. 当日范围和项目代码条件下推 SQL。
3. 不为每条规则分别查询数据库；所有规则共享一轮结果集。
4. 项目字典独立加载并缓存，不在大结果查询中执行不必要的重复关联。
5. 规则配对和比较在 C++ 内存完成。
6. 查询设置现有 ODBC 语句超时；超时后等待下一周期重试。
7. 当没有启用规则时不访问 LIS。
8. 最大 `REP_NO` 只用于发现新报告；最近 100 个编号回看、未完成报告复查和 15 段轮巡共同覆盖结果后补与旧值更正。固定编号回看不是“最近 100 分钟”，其时间跨度随报告量变化。

## 18. 测试与验证

### 18.1 已有自动化测试

- 六种比较运算符与近似相等；
- 负数、小数、前后空格和显式正号；
- 空值、`<10`、`1+`、`阴性` 等不可比较结果；
- 报告缺少任一项目时不匹配；
- 同一报告重复项目优先采用非空且 `ID` 更新的结果；
- 不同 `REP_NO` 之间不交叉配对。

### 18.2 存储与去重验证

- 相同规则、报告和结果只提醒一次；
- 结果变化后重新判断；
- 结果由命中变为不命中；
- 结果由不命中变为命中；
- 主程序重启后仍不重复提醒；
- 新规则首次扫描会提醒当天全部新命中，之后相同指纹不再重复提醒。
- 修改规则后，旧命中记录和观察指纹在同一事务中清除；
- 删除规则后，其命中记录和观察指纹同时清除；
- 单纯启停规则保留已有记录。

### 18.3 调度测试

- 固定 60 秒触发；
- 上一轮未完成时不重入；
- 无规则时不查询；
- 查询失败后下一周期可恢复；
- 页面关闭后扫描继续；
- 主窗口关闭后无异步回写。
- 仪器规则每分钟扫描该仪器当日目标项目；旧规则首次、规则变更、跨日和手动扫描执行当日全量，平时只读新增、回看、待复查和当前轮巡段。
- 报告先无明细、后新增明细，以及原明细 `RESULT` 从空变有值时均可命中；扫描失败不推进游标。
- 已完成旧报告更正由轮巡发现，不承诺一分钟内发现。

### 18.4 UI 测试

- 项目代码、中文名和英文名搜索；
- 同名不同代码可区分；
- 规则增删改、启停和持久化；
- 汇总提醒；
- 命中状态回写；
- 双击跳转常规报告；
- 字号变化和窗口缩放；
- 规则设置默认收起且可展开；
- 未处理悬浮窗展开、收起、点击跳转和自动隐藏；
- 悬浮窗拖动、跨显示器边界限制、位置持久化，以及点击后主窗口最大化；
- 新命中的通知区域提醒、提示音和任务栏闪烁；
- 跨日期未处理记录在重启后恢复。

## 19. 首版验收标准

满足以下条件视为首版完成：

1. `定时任务 -> 定时核查结果` 可打开单实例页面。
2. 用户可从 `LS_AS_ITEM` 搜索并选择两个项目，保存数值比较规则。
3. 规则和启用状态在程序重启后保留。
4. 主程序运行期间每分钟核查当日目标结果；仪器规则每轮扫描该仪器当日目标项目，旧规则继续采用增量与分段巡检，页面无需打开。
5. 同一 `REP_NO` 两侧结果满足关系时生成本地命中记录。
6. 空值和非纯数值结果不会误判为数值。
7. 首次扫描包含当天全部已审核和未审核报告；相同结果不会重复弹窗，结果变化后可重新核查。
8. 一轮多个命中只产生一个系统汇总提醒，未处理记录持续显示在置顶悬浮窗中。
9. 待处理列表支持标记处理和跳转常规报告，规则设置默认收起。
10. 对 LIS 全程只读，不执行新增、修改、删除或审核操作。
11. MinGW 主程序构建通过，核心数值解析、比较和报告内配对逻辑具有自动化测试。

## 20. 暂缓功能

以下功能不进入首版：

- 自定义扫描周期；
- 常量阈值和数学表达式；
- 三个及以上项目的组合规则；
- 定性结果顺序或文本相等规则；
- 对 `<10`、`>100` 等界限结果做区间推理；
- 跨 `REP_NO`、跨患者历史结果比较；
- 按专业组、病人类型等进一步限定规则；

## 仪器定向扫描补充（当前实现）

- 新建规则先通过常规报告共用的仪器选择器选定检验科室及仪器，持久化 `room_code / mach_code / mach_name`；旧 SQLite 规则新增列默认为空，仍按原全局路径工作，编辑旧规则时可主动选择仪器迁移。
- 同一 `ROOM_CODE + MACH_CODE` 的启用规则按项目代码并集执行一次当日查询，规则比较还会复核明细所属仪器。此路径每分钟回看该仪器当日全部目标结果，覆盖晚到的项目明细及原地修改的 `RESULT`，无需全局最大 `REP_NO` 游标和最近 100 号的时间窗口假设。
- 规则界面使用 `LS_AS_GROUP` 与 `LS_AS_GROUP_ITEM` 的 `ROOM_CODE + GROUP_CODE` 关联取得所选仪器项目，再与 `LS_AS_ITEM` 项目字典取交集；项目 A、项目 B 的下拉列表和代码精确匹配都只能使用交集内的项目。映射或字典加载失败、交集为空时禁止保存，不能回退到全字典。`LS_AS_ADDITEM` 不参与本流程。旧的未绑定仪器规则继续后台扫描，但修改保存前必须选择仪器并重新选择该仪器范围内的项目。
- 规则编辑器按“选择仪器 → 规则名称 → 项目 A/B（或固定值）→ 比较关系”组织；宽窗口采用双列，窄窗口逐行排布，按钮自动换行。项目选择仅在仪器项目与字典均加载完成后启用。
- 本地 SQLite 仅保存规则、提醒及旧规则扫描进度；LIS 数据库仍只读。多工作站规则共享与集中扫描不在本次范围。
- 短信、企业微信等外部通知；
- 命中筛选、表头排序、单元格复制和一键刷新；
- 自动修改结果、撤销审核或阻止报告发送；
- 命中记录导出和统计报表。

这些能力如需增加，应先补充独立业务口径和测试边界，不能直接扩展首版数值比较逻辑。
