# LIS Workbench 持续优化设计与路线图

本文档用于长期记录 LIS Workbench 的工程优化目标、风险、实施顺序和验收标准。它不是一次性审查报告，也不替代各业务模块设计文档；后续每次架构调整、稳定性治理和技术债清理都应在这里更新状态和结论。

## 文档信息

- 建立日期：2026-09-11
- 初始评估版本：`v2026.09.10.1`
- 当前优化范围：Win32 主程序、核心查询层、SQLite 本机存储、构建测试、安装与自动更新
- 暂不处理范围：Qt 前端；保留现状和已知问题记录，不投入当前优化资源
- 当前阶段：功能快速扩展后，进入稳定性、安全性和可维护性治理阶段
- 功能基线结论：用户已于 2026-09-11 确认当前功能达到满意状态，后续优化以保持现有功能和业务口径为硬约束
- 状态约定：`待处理 / 设计中 / 实施中 / 已完成 / 暂缓 / 不采纳`

## 一、项目现状与总体判断

LIS Workbench 当前已经形成较完整的 Windows LIS 工作台：包含检验结果查询、常规报告、输血结果、条码查询、标本签收、质控分析、门诊查询、常用电话及多类统计功能，并具备 Excel 导出、自动更新、安装包和部分崩溃诊断能力。

当前代码规模约为 4.46 万行 C/C++。Win32 是实际生产主线，Qt 前端仅覆盖部分查询、设置和趋势图。项目功能成熟度已经明显高于工程基础设施成熟度，后续优化应优先降低以下风险：

1. 后台线程与窗口生命周期不一致导致的偶发崩溃、错误消息投递和关闭卡顿。
2. 数据库密码、患者查询条件和安装目录权限带来的安全与隐私风险。
3. 自动化测试没有成为发布门禁，构建成功不能代表业务口径正确。
4. `search_core` 和多个 UI 模块持续膨胀，修改影响面越来越大。
5. SQL 拼接、查询超时、结果规模和一致性策略缺少统一约束。
6. 优化过程可能因过度分层和机械拆分产生过多文件、接口和跳转成本。

总体策略：**聚焦 Win32 主线，先稳定、再安全、补测试，最后按实际收益做最小必要重构。** Qt 暂时冻结；在 P0 和 P1 基础治理完成前，不建议继续大规模增加新的统计页面，也不进行面向未来假设的大规模架构重组。

在任何代码优化开始前，先通过 Git 固化当前满意版本，建立可验证、可发布、可回退的稳定基线。后续优化不得改变既有功能、数据库口径、页面行为和导出结果，除非对应优化项明确说明并获得确认。

## 二、优化原则

所有后续优化应遵守以下原则：

- LIS 业务数据库保持只读，优化不得扩大数据库写权限。
- 稳定性和患者数据安全优先于界面细节和新功能数量。
- 先建立测试保护，再拆分大文件或重写公共基础设施。
- 业务口径与 UI、线程、ODBC 细节分离，纯计算逻辑保持可独立测试。
- 严格控制新增代码文件和抽象层；默认在现有模块内完成局部整理，只有职责能够独立说明、可以独立测试或被多处复用时才新增文件。
- 不按固定模板机械拆分模块，不为每个类、状态或 helper 单独创建文件，不引入只有转发作用的 Service、Manager、Provider 层。
- 文件数量不是唯一指标：既避免数千行、多职责的巨型文件，也避免几十行一个文件的碎片化；以理解成本、修改影响面和测试能力作为判断标准。
- 小型模块原则上维持 `.cpp + .h`；中型模块通常控制在 2～4 个实现文件；超过约 1500～2000 行且职责明显混杂时，才评估拆分。
- 长任务必须可识别、可取消、可超时，窗口销毁后不得继续回写 UI。
- 日志默认不记录患者身份信息、数据库密码或完整查询 SQL。
- 配置、缓存、日志和程序二进制使用不同目录和最小权限。
- 每项优化必须有明确验收标准和回滚方式，避免长期处于“重构一半”的状态。
- Git `main` 必须代表可发布状态；所有优化使用短生命周期分支和可审查提交，禁止长期功能分支继续充当事实主干。
- 一个优化分支原则上只对应一个 OPT 项，不混入新功能、无关格式化或其他顺手重构。
- 保持 Windows 7 兼容需求可见；任何依赖升级都必须明确最低系统版本影响。

## 三、优先级定义

| 级别 | 含义 | 处理要求 |
| --- | --- | --- |
| P0 | 可能导致崩溃、数据泄露、程序被篡改或发布错误版本 | 原则上暂停相关扩展，优先修复 |
| P1 | 明显增加维护成本、查询风险或回归概率 | 应进入近期版本计划 |
| P2 | 中期架构和研发效率优化 | 在测试保护建立后分阶段实施 |
| P3 | 体验、规范和长期演进项 | 按实际收益滚动安排 |

## 四、优化总览

| ID | 优先级 | 主题 | 当前状态 | 主要目标 |
| --- | --- | --- | --- | --- |
| OPT-017 | P0 | Git 基线、分支与发布治理 | 已完成 | `main`、保护规则、CI 门禁和正式 tag 已对齐 |
| OPT-001 | P3 | Qt 已知异步风险记录 | 暂缓 | 冻结 Qt，不进入当前优化计划，未来恢复前先处理 |
| OPT-002 | P0 | Win32 后台任务统一治理 | 实施中 | 通用任务基础设施与首个高频页面迁移已启动 |
| OPT-003 | P0 | 数据库凭据安全 | 待处理 | 不再明文保存数据库密码 |
| OPT-004 | P0 | 安装目录权限隔离 | 待处理 | 普通用户不能修改 exe、Updater 和 DLL |
| OPT-005 | P0 | 日志脱敏与保留策略 | 待处理 | 日志不泄露患者身份和完整 SQL |
| OPT-006 | P1 | CI 测试发布门禁 | 已完成 | Windows CTest 门禁已配置并通过 push 与 PR 两种触发验证 |
| OPT-007 | P1 | 核心库平台边界准确化 | 待处理 | 修复声明与实现不一致，不追求过度拆分 |
| OPT-008 | P1 | ODBC RAII 与参数绑定 | 待处理 | 统一资源、参数、错误、超时和取消 |
| OPT-009 | P1 | 查询规模和一致性治理 | 待处理 | 避免无限查询和不恰当的 `NOLOCK` |
| OPT-010 | P1 | 自动更新信任链 | 待处理 | 从哈希完整性提升到发布真实性校验 |
| OPT-011 | P1 | 关键业务契约测试 | 待处理 | 保护统计口径、质控、配置迁移和更新回滚 |
| OPT-012 | P2 | `search_core` 渐进式职责治理 | 待处理 | 以最少新增文件降低核心修改影响面 |
| OPT-013 | P2 | 大型 UI 模块适度拆分 | 待处理 | 在职责清晰与文件碎片化之间保持平衡 |
| OPT-014 | P2 | Qt 冻结与维护边界 | 已确认 | 当前只维护 Win32，Qt 恢复需重新决策 |
| OPT-015 | P2 | 构建脚本和依赖治理 | 待处理 | 统一 CMake、Make、PowerShell 和 CI 行为 |
| OPT-016 | P3 | 文档与仓库治理 | 待处理 | 形成可靠的架构、变更和决策记录 |

## 五、当前范围说明与 P0 稳定性、安全性设计

### OPT-017：Git 基线、分支与发布治理

#### 已确认前提

- 用户已确认当前功能达到满意状态。
- 当前代码可以作为稳定基线候选，后续优化目标是提高稳定性、安全性、测试能力和可维护性，而不是重新设计业务功能。
- 当前实际开发分支为 `feat/quality-control-analysis-design`，其内容已经远超分支名称所表达的范围；`main` 明显落后于当前实际代码。
- 当前源码版本为 `v2026.09.10.1`，现有最新 Git tag 为 `v2026.06.30`，版本、tag 和实际发布基线需要重新对齐。

#### Git 工作模型

采用轻量主干开发，不引入长期 `develop` 或复杂 GitFlow：

```text
main
 ├─ feat/xxx
 ├─ fix/xxx
 ├─ refactor/xxx
 ├─ ci/xxx
 ├─ docs/xxx
 └─ hotfix/xxx
```

规则：

- `main` 永远代表当前可发布版本。
- 功能、修复、优化和文档使用短生命周期分支，通过 PR 合入 `main`。
- 一个分支只处理一个清晰主题；一个优化分支原则上对应一个 OPT 编号。
- 分支合入后删除，不保留已经完成的长期功能分支。
- 紧急修复从 `main` 或最近稳定 tag 创建 `hotfix/*`。
- 不在分支中同时进行大规模重命名、文件搬迁和行为修改。
- 不因优化需要创建额外 Git 流程文档；本节作为当前项目的统一 Git 约定。

#### 稳定基线固化步骤

以下步骤必须按顺序执行，不直接批量操作：

1. 记录当前候选基线提交、版本号和工作区文档改动。
2. 完成 Windows Release 构建、全部 CTest 和必要的现场烟雾测试。
3. 确认安装包、自动更新包和现有功能与现场满意版本一致。
4. 通过 PR 将当前事实主线合入 `main`，不直接覆盖或强推 `main`。
5. 在合并后的 `main` 创建与 `src/version.h` 完全一致的 annotated tag。
6. 保存 tag、提交哈希、编译器、依赖版本和发布产物之间的对应关系。
7. 验证能够从该 tag 重新构建，并完成一次回退演练。
8. 基线固化完成后，才从 `main` 创建第一个代码优化分支。

当前满意状态的候选代码提交为 `050fbcc`。最终基线应包含本路线图等必要文档，但不得夹带未经验证的程序行为变更。

#### 历史分支治理

- 逐个检查本地和远端分支是否已被候选基线包含。
- 已合并分支在确认无独立提交后删除本地和远端引用。
- 有保留价值但不再开发的检查点可转为带说明的 `archive/...` annotated tag，再删除分支。
- 尚有价值的未合并提交必须说明用途，并通过独立 PR 处理。
- 不允许为了“看起来整洁”而在未核对提交前批量删除分支。
- 长期活动分支原则上控制在 3～5 个以内。

#### 提交规范

统一采用清晰的 Conventional Commits 风格：

```text
feat(scope): ...
fix(scope): ...
refactor(scope): ...
test(scope): ...
docs(scope): ...
build(scope): ...
ci(scope): ...
chore(scope): ...
```

提交要求：

- 一个提交表达一个可解释、可回退的意图。
- 标题说明变更目的，避免 `update`、`fix stuff`、`完善代码` 等模糊描述。
- WIP 提交可以存在于个人分支，但合入前应整理为有意义的提交序列。
- 不强制所有 PR squash 成一个提交；有独立验证价值的重构步骤应保留。
- 纯移动或重命名尽量与行为修改分开，方便 Git 识别历史。

#### Tag 与发布规则

- `src/version.h` 是构建版本的唯一来源。
- 正式 tag 必须与程序版本完全一致，继续使用 `vYYYY.MM.DD` 或 `vYYYY.MM.DD.N`。
- 正式版本使用 annotated tag；条件具备后增加签名。
- 已发布 tag 不允许移动、覆盖或复用。
- tag 必须指向 `main` 上已经通过发布门禁的提交。
- CI 校验 tag、`src/version.h`、manifest、安装包和更新包版本一致。
- 每个正式发布记录源提交、构建环境、依赖版本、哈希和验证结果。

#### 分支保护与 CI

为 `main` 建立以下保护：

- 禁止 force push 和删除。
- 默认禁止直接 push，通过 PR 合入。
- Windows Release 构建和 CTest 为必需检查。
- 测试失败、版本不一致或发布包校验失败时禁止合并与发布。
- 普通 push 和 PR 只授予只读 GitHub Token；只有 tag 发布任务获得 `contents: write`。
- CI 不只匹配 `feat/*`；PR 必须完整验证，必要时覆盖 `fix/*`、`refactor/*`、`ci/*` 和 `docs/*`。

如果当前只有一名开发者，可以保留自审自合并，但仍必须经过 PR 和自动检查，以留下完整的决策与验证记录。

#### 仓库内容边界

必须纳入 Git：

- 源码、CMake、构建与打包脚本。
- 正式设计文档、数据库 schema 和 CI 配置。
- 完全脱敏并明确用于测试的数据。

禁止纳入 Git：

- `ClientConfig.ini`、数据库密码和真实连接字符串。
- 日志、崩溃转储和运行期 SQLite 数据库。
- 患者姓名、身份证、电话、条码、报告结果和未脱敏现场截图。
- 本地构建目录、安装包、IDE 私有状态和个人工具缓存。

被 `.git/info/exclude` 或 `.gitignore` 隐藏的正式设计文档应逐项判断；属于项目事实来源的文档必须正式提交，不能只存在于一台电脑。

#### 新增文件与 PR 约束

每个优化 PR 必须说明：

- 对应 OPT 编号和明确非目标。
- 新增、删除和重命名的文件数量。
- 每个新增文件的一句话职责及其必要性。
- 是否增加新的接口、抽象层或依赖。
- 测试结果、兼容性影响和回滚方式。

如果新增文件只包含少量转发代码、只有一个简单调用方且不能独立测试，默认不应新增。

#### 验收标准

- `main` 包含当前满意功能并能够重新生成正式发布物。
- `v2026.09.10.1` 或最终确认的对应版本 tag 与代码、manifest 和发布资产一致。
- `main` 受到保护，测试失败不能合并或发布。
- 历史分支经过逐项核对，不存在用途不明的长期活动分支。
- 任意优化都可以通过独立提交或 PR 回退，不依赖手工找回旧文件。
- Git 历史和仓库中不存在患者数据、数据库密码或现场配置。

#### 2026-09-11 基线固化记录

- `main` 基线提交：`e82553c230b27e7668d69460d2c3d5f5c68d3317`。
- 正式 annotated tag：`v2026.09.10.1`，与 `src/version.h` 完全一致。
- PR #1 使用 merge commit 合并；合并后 Windows Release 构建和 5 项 CTest 全部通过。
- `main` 已启用 PR、必需检查、对话解决、管理员约束、禁止 force push 和禁止删除保护。
- 后续程序行为优化从 `feat/opt-002-win32-task-governance` 独立分支开始。

### OPT-001：Qt 已知异步风险记录（暂缓）

#### 现状

`src_qt/trend_window.cpp` 的 `TrendWindow::loadTrendData()` 使用 `QtConcurrent::run`，后台闭包捕获 `this` 和 `points_` 引用。窗口在任务完成前关闭时，后台任务可能访问已销毁对象；后台线程直接写 `points_` 也使线程归属不清晰。

#### 当前决策

- Qt 版本暂时冻结，不开发、不优化、不做功能对齐，也不删除现有代码。
- 当前 P0 稳定性工作只覆盖 Win32 生产主线。
- Win32 优化不要求同步实现到 Qt，也不为兼容 Qt 提前增加抽象层。
- 公共业务逻辑仍避免无必要地继续绑定 UI 类型，但不以未来 Qt 迁移为理由扩大本次改造。
- 将本问题保留为已知风险；未来决定恢复 Qt 前，必须先重新评估并修复。

#### 未来恢复 Qt 的前置条件

- 形成明确的 Qt 产品目标、投入预算和 Windows 兼容范围。
- 修复异步对象生命周期和跨线程成员写入问题。
- 建立 Qt 独立构建与测试门禁。
- 未满足上述条件时，OPT-001 保持“暂缓”，不得混入 Win32 优化任务。

### OPT-002：Win32 后台任务统一治理

#### 现状

多个模块直接创建 `std::thread`，大量使用 `detach()`，并通过 `PostMessageW(HWND, ..., LPARAM(raw_pointer))` 回传结果。部分页面使用可连接线程，但在 `WM_DESTROY` 中同步 `join()`，可能因数据库连接或查询未结束而冻结关闭流程。

#### 目标设计

引入统一任务基础设施，例如：

```cpp
struct TaskToken {
    std::atomic<bool> canceled{false};
    std::uint64_t generation = 0;
};

template <typename Result>
class WindowTask;
```

该基础设施优先在现有公共文件中落地；确需新增时，原则上最多增加一个职责明确的 `.h + .cpp` 文件对，不为每个业务模块分别创建 Task、Service 或 Executor 文件。

统一约束：

- 页面状态持有任务句柄，禁止业务模块自行 `detach()`。
- 任务捕获不可变查询参数，不捕获页面状态裸指针。
- 完成消息携带拥有所有权的结果封装和 generation。
- 投递前校验窗口实例令牌，不能只依赖可能复用的 `HWND`。
- 页面销毁时只标记取消并释放 UI 所有权，不在 UI 线程等待网络任务。
- 新查询可以取消旧查询，旧结果统一丢弃。
- 应用退出时由任务管理器做有界等待，超时后安全结束。

#### 推进顺序

1. 先实现通用任务封装和示例测试。
2. 迁移查询模块和常规报告。
3. 迁移输血、质控、条码和统计模块。
4. 删除所有未经例外登记的 `detach()`。

#### 2026-09-11 首个实现切片

- 基线清点发现 16 个 Win32 源文件共有 23 处直接 `.detach()`；首个页面迁移后剩余 22 处，该数字作为后续收敛基准。
- 新增且仅新增一对通用基础设施文件 `window_task.h/.cpp`，使用 Windows 系统线程池执行工作，并通过 UI 线程专属的隐藏消息窗口投递拥有所有权的完成对象；系统模态消息循环同样能够正常派发。
- 每个页面持有 `WindowTask`，通过 generation 淘汰旧结果；页面销毁只取消令牌，不在 UI 线程等待后台查询。
- 首个迁移页面为门诊查询：移除 detached thread、窗口裸指针结果消息和手工 `new/delete` 结果协议。
- 新增一个基础设施测试目标，覆盖正常完成、工作异常、重复任务只接收最新结果，以及取消后不再回调。
- 本切片不改变 SQL、查询条件、排序口径、页面布局或 Qt 代码；也不尝试一次迁移全部模块。

#### 2026-09-11 第二个实现切片

- 查询模块的报告列表和项目明细两个可连接线程改由 `WindowTask` 托管，删除新查询与窗口销毁时的 UI 线程同步 `join()`。
- 常规报告页面的样本列表、项目明细、内嵌图像和弹窗图像四条后台链路统一迁移，不再通过窗口消息传递结果裸指针。
- 新的主查询会取消旧的项目明细和图像结果所有权；快速切换报告、清空选择或关闭窗口后，旧结果不再回写页面。
- 本切片没有新增文件、接口或依赖；继续复用现有结果结构、完成函数和 generation 校验，避免扩大行为变化。
- 完成本切片后，直接 `.detach()` 从基线 23 处降至 18 处，查询模块另有 2 个同步等待线程被移除。

#### 2026-09-13 第三个实现切片

- 输血 LIS 弹窗的报告、汇总和项目明细三条后台链路迁移到 `WindowTask`；新查询、清空选择和窗口销毁均会取消旧结果所有权。
- 质控查询与导入共用的后台链路完成迁移，保留原有缓存、Westgard 判定和状态汇总行为。
- 条码查询和 XLSX 导出从页面持有的可连接线程迁移；窗口销毁不再同步 `join()`，导出使用不可变行快照，不捕获 `BarcodeState*`。
- `WindowTaskContext` 增加协作取消和受 generation 保护的 UI 进度回调，用于安全保留条码导出进度显示；对应行为纳入原有 `window_task_tests`，没有增加测试文件。
- 本切片没有新增文件或依赖，只扩展现有任务基础设施；完成后直接 `.detach()` 从 18 处降至 14 处，并移除条码模块 2 个 UI 同步等待线程。

#### 2026-09-14 第四个实现切片

- 标本签收的单条条码查询与已签收列表查询迁移到同一个页面级 `WindowTask`，继续保持两类查询互斥和原有结果展示行为。
- TAT 统计的查询、专业组初始化和 XLSX 导出分别迁移到页面持有的三个 `WindowTask`，避免互相覆盖任务代际；导出继续使用提交时的明细快照。
- 两个页面在窗口销毁时先撤销窗口状态属性，再取消全部任务；已排队的结果由现有 generation 校验丢弃，不再向已销毁页面投递自定义完成消息。
- 本切片没有新增文件、任务基础设施接口或依赖；完成后直接 `.detach()` 从 14 处降至 9 处。

#### 验收标准

- 源码默认不存在直接调用 `.detach()` 的业务模块。
- 查询中关闭页面不会卡住主线程。
- 关闭后完成的任务不会向已销毁或新复用窗口投递结果。
- 快速连续查询只显示最后一次请求结果。
- 异常路径不泄漏结果对象。

### OPT-003：数据库凭据安全

#### 现状

数据库用户名和密码保存在程序目录的 `ClientConfig.ini`，其中密码为明文。连接字符串还设置了 `persist security info=True`。

#### 目标设计

- 非敏感数据库设置保留在配置文件。
- 密码使用 Windows Credential Manager；如需兼容旧系统，可使用 DPAPI 作为实现备选。
- 配置仅保存凭据引用标识，不保存可直接使用的明文密码。
- 首次读取旧配置时执行一次迁移；迁移成功后删除旧 Password 值。
- 数据库账户使用最小只读权限，不允许使用数据库管理员账户部署。
- 内存和日志中不输出完整连接字符串。

#### 验收标准

- 新保存的配置文件中不存在明文 `Password=`。
- 旧配置能够自动迁移且不要求用户重新录入。
- 普通日志、错误框和崩溃信息不包含密码。
- 提供凭据清除和重新录入流程。

### OPT-004：安装目录权限隔离

#### 现状

NSIS 当前对整个安装目录递归授予普通用户修改权限，导致配置可写的同时，exe、Updater 和 DLL 也可被普通用户替换。

#### 目标目录

```text
%ProgramFiles%\LISWorkbench\          只读程序文件
%ProgramData%\LISWorkbench\          全机配置、模板、共享缓存
%LocalAppData%\LISWorkbench\         用户日志、临时文件、窗口状态
```

需要结合现场是否多用户登录决定配置属于 ProgramData 还是 LocalAppData。数据库凭据不放入上述普通文本目录。

#### 验收标准

- 普通用户无法覆盖主程序、Updater 和 DLL。
- 普通用户仍能保存允许修改的配置和导入文档。
- 安装升级保留既有配置并支持从旧目录迁移。
- 卸载器明确说明保留或删除用户数据的策略。

### OPT-005：日志脱敏与保留策略

#### 现状

查询层会记录完整 SQL，其中可能包含姓名、病人号、身份证、电话和条码。日志按日创建，但缺少自动清理、容量上限和敏感字段统一脱敏。

#### 目标设计

日志事件采用结构化摘要：

```text
query=report duration_ms=120 rows=35 filters=name:true,barcode:false request_id=...
```

约束：

- 默认禁止记录完整 SQL 和完整连接字符串。
- 患者标识只记录是否提供或不可逆短哈希，不记录原值。
- DEBUG SQL 诊断必须由显式开关启用，并显示隐私提示和自动过期时间。
- 日志滚动应同时受天数和总容量限制。
- 崩溃报告遵守同样的脱敏策略。

#### 验收标准

- 自动测试扫描日志，确认测试患者姓名、身份证和密码不出现。
- 默认日志保留期和容量上限可配置且有安全默认值。
- 现场排障仍能通过请求 ID、错误码、耗时和查询类型定位问题。

## 六、P1 工程质量设计

### OPT-006：CI 测试发布门禁

#### 现状

CMake 已定义测试，但 GitHub Actions 只构建和打包，没有执行 `ctest`。Makefile 的 `test` 目标仍为占位实现。

#### 目标流水线

```text
configure
  -> compile
  -> unit tests
  -> static analysis / warnings
  -> package
  -> package verification
  -> sign
  -> publish
```

约束：

- 任一测试失败时不得生成正式发布资产。
- 普通 push 和 PR 使用只读 GitHub Token。
- 只有 tag 发布任务获得 `contents: write`。
- 构建产物必须记录编译器、依赖版本和源提交。

#### 验收标准

- CI 明确展示所有测试名称和结果。
- `make test` 或统一脚本真实运行测试。
- 人为制造一个失败断言能够阻止发布阶段执行。
- Release tag、应用版本和包内版本三者一致。

### OPT-007：核心库平台边界准确化

#### 现状

CMake 将 `search_core` 描述为平台无关核心库，但它直接包含使用 Win32 API 的 `log.cpp`。部分测试通过直接编译 `search_core.cpp` 绕开完整核心库，导致真实链接边界没有被测试覆盖。

#### 目标设计

- 不立即创建 `search_domain / search_db_odbc / search_services` 等多层 target。
- 先把 Win32 专用日志实现从当前核心 target 的平台无关构建路径中隔离；优先通过 CMake 条件源文件或一个小型平台适配实现解决。
- 仅将已有明确单元测试的纯计算逻辑保持为非 Windows 可编译，不要求所有 ODBC 查询跨平台。
- 如果未来确实需要拆分 target，每次只新增一个职责明确的 target，并验证它减少了生产和测试构建差异。
- 测试链接真实库 target，不直接重复编译生产 `.cpp` 文件。

#### 验收标准

- 纯领域库可以在 macOS/Linux 原生编译并运行测试。
- `search_core` 的平台能力描述和实际实现一致，不以批量创建新 target 作为完成标志。
- 测试与生产使用相同的编译定义和公共接口。

### OPT-008：ODBC RAII、参数绑定与错误模型

#### 现状

查询主要使用 SQL 字符串拼接与 `SQLExecDirectW`，没有统一使用参数绑定；连接和 statement 在每个函数中手工管理。

#### 目标设计

```cpp
class OdbcEnvironment;
class OdbcConnection;
class OdbcStatement;

struct DbError {
    std::string operation;
    std::string sql_state;
    long native_code = 0;
    std::string safe_message;
};
```

能力要求：

- 构造和析构自动管理 ODBC handle。
- 所有筛选值使用 `SQLBindParameter`。
- 统一登录超时、查询超时和取消。
- 区分用户可见安全错误与诊断错误。
- 不在错误文本中携带完整 SQL 或连接字符串。
- 支持可测试的数据访问接口和假实现。

#### 验收标准

- 用户输入不再直接拼接到 SQL 字符串。
- 任意异常返回路径都不会泄漏 ODBC handle。
- 可通过测试模拟超时、断网、空值和 SQL 错误。

### OPT-009：查询规模、性能和一致性

#### 现状

部分查询设置 `TOP`，但常规报告、条码、门诊和统计功能存在无限结果模式。项目广泛使用 `WITH (NOLOCK)`，对统计口径和患者详情可能产生脏读、重复或遗漏。

#### 目标设计

- 每个查询定义默认上限、最大范围和允许的无限模式。
- 大结果查询使用分页、流式读取或先汇总后取明细。
- 查询页面显示结果是否被截断。
- 为统计查询定义一致性等级：实时近似、普通只读、一致性快照。
- 只有明确接受脏读的查询才能使用 `NOLOCK`，并在代码中说明理由。
- 根据真实执行计划和现场数据量建立索引建议，不凭 SQL 文本盲目加索引。
- 记录查询类型、耗时和行数，形成慢查询基线。

#### 验收标准

- 所有数据库查询都有语句超时。
- 所有无限结果查询经过登记并提供用户提示或分页。
- 关键统计在并发写入期间有明确且可复现的一致性语义。
- 现场慢查询能够通过匿名指标定位。

### OPT-010：自动更新信任链

#### 现状

更新包当前校验 size 和 SHA-256，但哈希值来自同一份未签名 manifest，因此主要保证传输完整性，不能独立证明发布者身份。更新源允许 HTTP，CI 下载外部 LabelPrint 包时未校验固定哈希。

#### 目标设计

- 外网更新强制 HTTPS；内网共享目录作为独立受控通道。
- manifest 使用内置公钥验证的数字签名。
- 主程序、Updater、安装包使用 Authenticode 签名。
- package URL 默认必须与 manifest 同源，不接受任意绝对 URL。
- 更新包只允许白名单文件和安全相对路径。
- 外部依赖下载校验固定 SHA-256。
- Updater 验证目标目录、应用标识、版本递增和包签名后再替换。

#### 验收标准

- 修改 manifest 或 zip 任意字节都会被拒绝。
- 使用攻击者自制 manifest 和对应哈希仍会因签名不正确被拒绝。
- 更新失败能够完整回滚，配置和患者相关本机数据不丢失。
- 普通用户不能用同名假 Updater 替换正式更新器。

### OPT-011：关键业务契约测试

优先测试范围：

1. TAT、输血、大量输血、HIV、急诊、免疫重复统计口径。
2. Westgard 规则、批号生效时间、靶值和 SD 匹配。
3. 配置 Unicode、旧 INI 迁移、凭据迁移。
4. SQL 参数边界、空值、特殊字符、重复记录和日期边界。
5. XLSX 大数据、跨工作表、取消和临时文件清理。
6. manifest 解析、版本比较、签名验证、替换失败回滚。
7. 页面销毁、快速重复查询和旧结果丢弃。

测试分层：

- 单元测试：纯函数和领域规则。
- 契约测试：数据访问接口与脱敏小型数据库快照。
- 组件测试：SQLite、XLSX、更新包。
- 烟雾测试：Windows 7、Windows 10、Windows 11 实机或虚拟机。

验收目标不使用单一覆盖率数字替代业务判断；关键规则必须覆盖正常、边界和失败路径。覆盖率可作为趋势指标，不能作为唯一完成标准。

## 七、P2 架构演进设计

### OPT-012：`search_core` 渐进式职责治理

当前不采用一次性建立 `core/common/infrastructure/services` 等多层目录的方案，也不按业务域批量创建大量文件。`search_core` 的治理以最小改动、真实收益和测试保护为前提。

#### 默认策略

- 暂时保留 `search_core.cpp/h` 作为兼容入口，不进行全仓库搬迁。
- 优先在现有文件内整理命名、重复 helper 和局部区域，不因行数本身立即拆分。
- 只有同时满足以下条件之一时才提取新文件：
  - 一组纯业务规则可以脱离 ODBC 和 UI 独立测试；
  - 同一实现已被两个或以上模块重复使用；
  - 一项职责有稳定边界，后续修改原因与原文件明显不同；
  - 不提取会持续扩大高风险修改范围。
- 新文件必须能用一句话说明唯一职责；无法说明时不拆。
- 每次重构原则上只引入一个新边界，并同步删除或收缩旧实现。

#### 允许优先提取的候选

- ODBC handle 的 RAII 管理和参数绑定：多个查询共同依赖，且能显著改善资源安全。
- 后台任务生命周期封装：多个 Win32 页面共同依赖，且直接降低崩溃风险。
- 已有明确测试的统计纯计算：仅在继续增长或出现重复时提取。

#### 暂不提取的内容

- 只在单个函数中使用的短 helper。
- 只做一层调用转发的 Service、Manager、Repository 接口。
- 为未来 Qt 迁移预设、当前 Win32 不需要的抽象。
- 仅为让单个文件行数下降而进行的无职责改善拆分。

#### 验收标准

- 拆分后完成同一业务修改所需打开和修改的文件没有明显增加。
- 新增边界有独立测试或至少两个明确调用方。
- 不形成新旧两套长期并存的公共接口。
- 编译依赖和修改影响面下降，而不是仅把代码移动到更多文件。

### OPT-013：大型 UI 模块适度拆分

UI 模块不采用固定的六文件模板。默认结构仍为 `xxx_module.cpp + xxx_module.h`，能够在一个文件中清晰维护时不拆分。

#### 拆分触发条件

同时观察职责和规模，而不是只看行数：

- 超过约 1500～2000 行并仍持续增长；
- 同一文件同时处理窗口生命周期、复杂计算、持久化和多种导出；
- 修改一个局部功能经常引发无关区域冲突或回归；
- 某部分可以独立测试、复用或由独立生命周期管理。

#### 文件数量约束

- 小型模块：原则上 2 个文件，即 `.cpp + .h`。
- 中型模块：通常不超过 2～4 个实现相关文件。
- 大型模块：按实际职责逐次提取，每次通常只新增 1 个文件对。
- 只被当前 `.cpp` 使用的简单状态、布局函数和 helper 默认留在匿名命名空间。
- 不为每个窗口状态、Presenter、Service 或 Exporter 机械创建独立文件。

#### 推荐拆分方式

优先提取风险独立且收益明确的职责，例如：

- 已经形成独立算法的统计计算；
- 可复用的 XLSX 或图片导出实现；
- 需要单独生命周期管理的后台任务；
- 独立的 SQLite 持久化层或图表渲染器。

窗口创建、控件布局、简单排序和消息路由可以继续共存在模块 `.cpp` 中，只要职责仍可快速定位。

优先拆分顺序：

1. `quality_control_module.cpp`
2. `regular_report_module.cpp`
3. `blood_module.cpp`
4. `barcode_module.cpp`
5. `search_core.cpp`

该顺序只是“需要评估的顺序”，不代表必须拆分。评估后没有明确收益时应保持原状。

### OPT-014：Qt 冻结与维护边界

当前决策已经明确：Win32 是唯一生产与优化主线，Qt 版本暂时不处理。

执行规则：

- 不为 Qt 增加新功能、修复一般问题或做 Win32 功能同步。
- Win32 重构不要求同步修改 Qt；如果公共接口变化导致 Qt 无法构建，可暂时关闭 Qt 构建或做最小兼容调整，不借机扩展 Qt。
- 不因假设未来迁移到 Qt 而增加当前不需要的目录、接口或抽象层。
- 保留 `src_qt` 和已知问题记录，避免丢失已有探索成果。
- 只有用户明确重新启动 Qt 方向时，才重新制定产品目标、兼容范围、投入预算和恢复计划。

本项决策状态为“已确认”。未来恢复 Qt 时应新增独立 ADR，而不是直接改变当前 Win32 优化范围。

### OPT-015：构建脚本与依赖治理

目标：CMake 是唯一构建事实来源，PowerShell、Makefile 和 CI 只负责传参和编排。

建议：

- 统一 target、配置、测试和包路径命名。
- `make test`、PowerShell test 和 CI 运行同一 CTest 集合。
- 明确 SQLite、LabelPrint、Qt 和 NSIS 的版本来源与校验方式。
- 第三方源码记录版本、许可证和升级流程。
- MSVC 与 MinGW 都启用合理警告；新增代码逐步做到 warnings-as-errors。
- 增加 clang-format 或格式检查，但避免首次引入时全仓库无关重排。
- 增加 clang-tidy、依赖检查和可选 AddressSanitizer/UBSan 构建。

## 八、P3 文档与流程治理

### OPT-016：文档与仓库治理

建议长期文档保持精简，只有内容持续增长并且具备独立维护价值时才建立子目录：

```text
README.md                         项目定位、快速构建和文档索引
PROJECT_STRUCTURE.md              当前代码结构
PROJECT_OPTIMIZATION_ROADMAP.md    本文档，持续优化主清单
CHANGELOG.md                       用户可感知版本变化
SECURITY.md                        凭据、日志、更新和漏洞处理
docs/adr/                          少量重大架构决策记录，按需创建
```

需要处理的现状：

- 部分设计文档仅存在本地并被 `.git/info/exclude` 或 `.gitignore` 排除，应判断是否正式纳入版本库。
- README 承担了过多模块实现细节，应逐步拆分成文档索引。
- 已失效的 placeholder、历史计划和重复脚本应登记后清理，不能长期保持真假难辨。
- 每次重大优化必须同步更新本文档和对应 ADR。

## 九、阶段路线图

### 阶段 0：Git 稳定基线固化

目标：把当前已确认满意的功能固化为可构建、可发布、可回退的唯一基线。

- 完成 OPT-017 的构建、测试、PR 合并和 tag 对齐。
- 将当前事实主线安全合入 `main`，不使用强制覆盖。
- 逐项核对历史分支，保留未合并价值后再清理。
- 配置 `main` 分支保护和发布门禁。
- 记录稳定发布物与源提交、版本、依赖和哈希的对应关系。

完成条件：`main` 和正式 tag 能重建当前满意版本，并通过既定构建、测试、发布和回退验证。

在本阶段完成前，不启动影响程序行为的重构。文档、CI 门禁准备和只读核查可以先行。

### 阶段 A：Win32 稳定性止血

目标：消除 Win32 生产主线中已识别的高风险异步生命周期问题。

- 设计并落地 OPT-002 的任务基础设施。
- 先迁移查询、常规报告、输血、质控四个高频页面。
- 为窗口关闭和重复查询增加回归测试。
- 为 ODBC 增加基础查询超时。
- OPT-001 保持暂缓，不把 Qt 工作混入本阶段。

完成条件：高频页面不再使用无托管 detached thread，压力关闭测试通过。

### 阶段 B：安全与隐私

目标：让凭据、日志、安装目录和更新链满足生产部署的基本安全要求。

- 完成 OPT-003、OPT-004、OPT-005。
- 完成 OPT-010 的 HTTPS、同源和依赖哈希部分。
- 建立 `SECURITY.md` 和部署账户权限说明。

完成条件：程序目录只读、密码不落明文、默认日志无患者身份信息。

### 阶段 C：质量门禁

目标：任何正式发布都经过自动测试和包验证。

- 完成 OPT-006、OPT-007。
- 启动 OPT-008 和 OPT-011。
- 建立 Windows 多版本烟雾测试清单。

完成条件：测试失败无法发布；纯领域核心可以在非 Windows 环境测试。

### 阶段 D：模块化重构

目标：在测试保护下，以最少新增文件逐步降低大文件和共享核心的修改风险。

- 完成 OPT-012、OPT-013。
- 只统一确实被多个模块重复使用的数据库访问、错误和任务接口。
- 每次只迁移一个业务域，避免全仓库重写。
- 每次拆分前记录新增文件数、删除或收缩内容、独立测试价值和调用方。
- 如果拆分增加了导航和修改成本，应停止或回退，而不是继续完成预设目录结构。

完成条件：高风险修改影响面下降，且项目没有因治理产生大量微型文件、空转接口或重复架构。

### 阶段 E：Qt 方向重新评估（当前不启动）

当前状态：冻结，不纳入本轮持续优化计划。

只有用户未来明确要求恢复 Qt 时，才启动以下工作：

- 重新确认 Qt 的产品价值、现场兼容要求和维护成本。
- 制定可度量的功能对齐矩阵和退出条件。
- 优先修复 OPT-001，再恢复功能开发。
- 单独评估是否继续、重建或归档现有 Qt 实验代码。

## 十、每项优化的工作模板

后续新增优化项时，复制以下模板：

```text
### OPT-XXX：标题

- 优先级：P0/P1/P2/P3
- 状态：待处理
- 负责人：
- 计划版本：
- 关联 issue/PR：

背景：
当前风险：
目标设计：
非目标：
实施步骤：
兼容性影响：
数据迁移：
回滚方案：
测试方案：
验收标准：
最终结果：
```

状态更新规则：

- 进入开发前补充负责人、计划版本、非目标和回滚方案。
- 合并代码后不能立即标记“已完成”；先完成自动测试和现场验证。
- 标记“已完成”时记录提交或 PR、验证环境和遗留问题。
- 不采纳某项建议时记录原因，避免未来重复讨论。

## 十一、版本发布检查清单

每次正式发布至少确认：

### 代码与测试

- [ ] 工作区没有误提交的配置、日志、数据库或患者数据。
- [ ] 当前提交位于受保护的 `main`，或来自指向 `main` 的正式 tag。
- [ ] 本次 PR 只对应一个明确主题或 OPT 编号。
- [ ] 新增文件数量及必要性已经在 PR 中说明。
- [ ] Windows Release 构建成功。
- [ ] CTest 全部通过。
- [ ] 关键统计口径测试通过。
- [ ] 新增后台任务通过关闭和重复查询测试。
- [ ] 没有新增未经登记的 detached thread。
- [ ] 新增代码文件均有明确独立职责，没有机械拆分或空转抽象。

### 安全与隐私

- [ ] 日志样本不包含密码、身份证、姓名、电话和完整条码。
- [ ] 安装目录 ACL 符合设计。
- [ ] 更新包、manifest 和签名验证通过。
- [ ] 外部依赖版本与哈希一致。
- [ ] 数据库账户仍为最小只读权限。

### 兼容性与部署

- [ ] 版本号、tag、安装包和 manifest 一致。
- [ ] 正式 tag 为不可变的 annotated tag，并指向通过门禁的 `main` 提交。
- [ ] 发布记录包含源提交、构建环境、依赖版本和产物哈希。
- [ ] 配置迁移不会覆盖现场设置。
- [ ] 更新失败回滚验证通过。
- [ ] Windows 7/10/11 目标环境完成相应烟雾测试。
- [ ] CHANGELOG、README 和相关设计文档已更新。

## 十二、度量指标

优化效果使用趋势衡量，不以单个数字机械考核：

- 稳定性：崩溃次数、窗口关闭卡顿、后台任务遗留数。
- 查询性能：按查询类型统计 P50/P95 耗时和返回行数。
- 发布质量：CI 失败率、发布后回滚率、现场紧急修复次数。
- Git 健康度：长期活动分支数量、未对齐版本 tag 数量、绕过 PR 的 `main` 提交和无法追溯来源的发布物数量。
- 测试质量：关键业务规则覆盖情况、缺陷是否有回归用例。
- 可维护性：超过 1500/2000 行且多职责的文件数量、新增文件数量、完成一次业务修改需要触碰的文件数量、核心头文件依赖数量和全量编译时间。
- 安全性：明文敏感配置数量、日志脱敏扫描结果、未签名发布物数量。
- 结构克制：微型文件、单调用方空转接口、重复抽象和新旧实现并存的数量。

指标不得记录患者身份或可反向识别的查询内容。

## 十三、初始审查验证记录

2026-09-11 初始审查：

- MinGW Windows 全量构建成功，包含主程序、Updater 和测试目标。
- 本机已有 CTest 中 4 个测试通过；`massive_transfusion_core_tests` 因完整 `search_core` 链接到 Win32 专用 `log.cpp` 而未能在 macOS 原生构建。
- 初始审查时 GitHub Actions 没有执行 CTest；现已在打包前构建并运行全部 5 个测试目标。
- 2026-09-11 GitHub Actions 的 push 与 PR 两次 Windows 工作流均通过，耗时分别约 1 分 56 秒和 2 分 08 秒，确认测试失败会阻断后续打包步骤。
- Makefile 的 `test` 目标仍为占位跳过。
- 发现 Qt 趋势窗口异步闭包捕获页面对象和成员引用。
- 2026-09-11 决定冻结 Qt；该问题降为已知暂缓项，不占用当前 Win32 优化资源。
- 发现大量 Win32 后台任务使用 detached thread 和裸指针窗口消息。
- 数据库密码仍以明文写入 `ClientConfig.ini`。
- 安装器递归放宽整个安装目录的普通用户修改权限。
- 默认调试日志链路可能写入包含患者筛选条件的完整 SQL。
- 当前评估只进行代码、构建、测试和文档检查，没有连接现场 LIS 数据库，也没有执行 Windows 实机 UI 压力测试。

## 十四、下一次更新建议

下一次维护本文档时，优先补充：

1. OPT-017 稳定基线验证结果、最终基线提交和 tag。
2. 历史分支逐项核对结果及保留、归档、删除决定。
3. OPT-002 的最小文件增量技术方案或原型结论。
4. Windows 7 对 Credential Manager、DPAPI、签名和新 TLS 方案的兼容验证。
5. 医院现场数据库只读账户权限清单。
6. 慢查询基线和主要表的脱敏执行计划。
7. 当前文件规模、职责混杂程度和典型修改触点基线，用于判断哪些文件真正值得拆分。

## 十五、路线图决策记录

### 2026-09-11：确认当前功能为满意基线候选

- 用户确认当前功能已经满意。
- 后续优化以保持现有业务功能、查询口径、页面行为和导出结果为硬约束。
- 在任何行为重构前，先执行 OPT-017，将当前事实主线固化到 `main` 和正式 tag。
- Git 治理成为阶段 0 和其他所有优化项的前置条件。

### 2026-09-11：冻结 Qt，聚焦 Win32

- Qt 暂时不开发、不优化、不做功能对齐。
- Qt 已知风险保留记录，但从当前 P0 工作中移除。
- 当前优化不得以未来 Qt 迁移为理由增加无实际收益的抽象。

### 2026-09-11：限制优化过程中的文件和抽象增长

- 放弃按固定目录和固定文件模板批量拆分的思路。
- 默认保留 `.cpp + .h` 的简单模块结构。
- 新文件必须具备独立职责、独立测试价值、多处复用价值或明确的风险隔离收益。
- 以理解成本和修改影响面衡量可维护性，不以文件越多或单文件越小作为目标。
